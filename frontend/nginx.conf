server {
    listen 80;

    # Весь трафик приходит с префиксом /apple-tree-diseases/ от главного nginx
    location /apple-tree-diseases/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /index.html;
    }
}
